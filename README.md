# .NET 学习

```bash
# 编译并运行
dotnet build
dotnet run
```

添加到.sln文件中：

```bash
# 添加现有项目到解决方案
dotnet sln add <project_path>

# 创建新的解决方案
dotnet new sln
```

## 创建 WPF 应用程序

```bash
# 创建 WPF 应用程序
dotnet new wpf -o MyWpfApp
```
