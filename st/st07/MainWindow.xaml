<Window
  x:Class="st07.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="clr-namespace:st07"
  Title="BMI Calculator"
  Height="200"
  Width="200">

  <!-- 设置数据上下文 -->
  <Window.DataContext>
    <local:ViewModel/>
  </Window.DataContext>

  <!-- 布局网格 -->
  <Grid>

    <!-- 定义行列 -->
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <!-- 身高输入 -->
    <Label
      Grid.Row="0"
      Grid.Column="0"
      Content="Height (feet)"/>
    <TextBox
      Grid.Row="0"
      Grid.Column="1"
      Text="{Binding FeetHeight, UpdateSourceTrigger=LostFocus}"
      Margin="0,0,5,0"/>

    <!-- 体重输入 -->
    <Label
      Grid.Row="1"
      Grid.Column="0"
      Content="Weight (pounds)"/>
    <TextBox
      Grid.Row="1"
      Grid.Column="1"
      Text="{Binding PoundsWeight, UpdateSourceTrigger=LostFocus}"
      Margin="0,0,5,0"/>

    <!-- 计算BMI按钮 -->
    <Button
      Grid.Row="2"
      Grid.Column="1"
      Content="Calculate BMI"
      Command="{Binding CalculateBMICommand}"
      Margin="10,10,10,0"/>

    <!-- BMI显示 -->
    <Label
      Grid.Row="3"
      Grid.Column="0"
      Content="BMI"
      Margin="0,10,0,0"/>
    <TextBlock
      Grid.Row="3"
      Grid.Column="1"
      Text="{Binding Bmi}"
      FontWeight="Bold"
      FontSize="16"
      Margin="0,10,0,0"/>
  </Grid>
</Window>